# Machine-specific darwin configuration for mb5619.
#
# Configuration shared by every machine lives in modules/darwin/; mkDarwin
# (lib/default.nix) composes those modules together with this one, so there is
# no need to import them here. Nothing in the repo is machine-specific yet.
{ ... }:
{
}
